/**
 * Input (required): hta, bbdrop, cslength, htlength, stack, reach
 * Optional:
 * - sta (for ST-HT compat check)
 * 
 * Checks:
 * - TODO
 */

function Calculator() {
  return (
    <div><form>Stack: <input type="text" /></form></div>
  );
}

export default Calculator;
